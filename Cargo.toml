[package]
name = "herdr-nav"
version = "0.1.0"
edition = "2024"
description = "Pane, tab, and workspace navigation plugin for herdr"
license = "MIT"

[lints.rust]
unsafe_code = "forbid"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
lto = true
strip = true
